#Simple Restapi with django rest_framework
<hr>
<ol>
<li>clone the repository</li>
>git clone https://github.com/andromedaismynexthome/RestApi_with_django.git
<li>u get a file name RestApi_with_django </li>
<li>go in that file</li>
>cd RestApi_with_django
<li>now create virtual environment </li>
> python3 -m venv env
<li>now activate virtual environment</li>
> env/Scripts/activate
<li>Now install depencies</li>
> pip install django && djangorestframework
<li>now everything is set and now run server</li>
>cd core && python manage.py runserver
</ol>